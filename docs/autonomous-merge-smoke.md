# Autonomous merge smoke test

This file exists only to create a small, safe pull request used to verify that
the sudo-ai agent can merge a PR autonomously through its `github.merge_pr`
tool — which merges only once the PR's required CI checks are green.

Safe to delete.
