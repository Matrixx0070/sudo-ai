# FRONTIER — machine-proposed capability directions (AL10.1)

Proposal engine output. The system suggests; Frank decides (AL10.5). Ranked by est. value / est. cost.
Statuses move only by human decision. The backing store is `frontier_entries` (frontier-ledger.ts);
this file is its human-facing mirror (regenerated by `FrontierLedger.syncMarkdown`).

Seed entries below are REAL observations from the 2026-07-28 campaign evidence (no synthetic filler);
each cites where the signal was actually recorded.

## Open

| # | signal | proposed capability | value | cost | source | evidence |
|---|--------|--------------------|-------|------|--------|----------|
| 1 | Three independent CI timeout flakes in one day (tests/acp/session-load, tests/workflows timing, tests/memory/chunk-contradiction R-1 — all 15s timeouts under parallel runner load) | Raise the CI runner's vitest testTimeout (or shard the suite) so runner-load timeouts stop forcing manual reruns | 4 | 1 | failure-cluster | docs/AGENTIC_LADDER_STATUS.md items 21+27 rerun notes; session-state 2026-07-28 "3rd runner-load flake today; pattern = timeouts under parallel CI load, not code" |
| 2 | The admin dashboard HTML sits at 56.7KB against a 30KB design target, ratchet-raised twice (53.1KB→54.3KB ceiling 2026-07-19, →57.9KB 2026-07-28) with the cause noted as duplication (F101 concern) | Split dashboard-html.ts into lazily-fetched panel fragments (server-side include or per-panel endpoints) and retire the 30KB debt instead of re-ratcheting | 3 | 3 | signals | tests/gateway/admin-dashboard-route.test.ts DASH-10 comments (both explicit raises documented in-test); CAS_WIRING_QA Q-1/A-1 ruling |
| 3 | The AL6 policy resolver's shed policy computes+logs on live traffic but has no promoted consumer — the seam is proven in tests yet idle in prod until the shadow window closes | After the ≥3-day AL6.5 shadow window: run the ledger's comparison query and promote (or retire) the shed policy with cited numbers — converting a built capability from idle to earning | 4 | 2 | eval-saturation | docs/AGENTIC_LADDER_STATUS.md item 20 (AL6.5 comparison SQL + promotion rule); item 21 (knob 5 consults-but-applies-legacy) |

## Decided

(none yet — the first quarterly review pack delivers these to Frank)
