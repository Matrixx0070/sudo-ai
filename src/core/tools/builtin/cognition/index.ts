/**
 * Cognition tool category — placeholder index.
 * Reserved for future cognitive/reasoning tools.
 */
import type { ToolRegistry } from '../../registry.js';

export function registerCognitionTools(_registry: ToolRegistry): void {
  // No tools registered yet — category reserved for future use.
}
