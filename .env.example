# SUDO-AI environment template moved.
# Canonical template: config/.env.example
# Copy config/.env.example to config/.env and fill in real values.
# config/.env is gitignored and MUST NOT be committed.
