# SIGNALS

Evidence of the system being used beyond its intended design (Engineering
Doctrine §9). Format: `date | observation | evidence | possible opportunity`.

2026-07-31 | The agent has at least three distinct memory-storage lanes and picks between them nondeterministically for the same "remember this" request: knowledge.graph (kg_nodes), meta.self-modify (file-based MEMORY.md-style writes), and MindDB.storeChunk 'learning' facts — plus the structured-memory API used by Drive/NotebookLM lanes. | Unified event log (/v1/events), three consecutive live probes 2026-07-31 14:38Z / 14:51Z / 15:11Z: identical "remember" prompts produced tool.completed for knowledge.graph, then meta.self-modify+system.exec, then knowledge.graph again. | Consolidate to one memory-write API (capability > feature) or at least one routing policy; until then, event instrumentation and invariant enforcement must cover every lane separately — each new lane is an uninstrumented blind spot by default.

2026-08-06 | The self-improvement engine treats a hand-written keyword table as a learning sink: it emits "# Apply these with meta.self-modify to src/core/agent/intent-classifier.ts" and tunes the regex/keyword intent classifier instead of adjusting a prompt or a model choice. The system is therefore using a piece of *scaffolding* as its primary place to store learned routing knowledge. | src/core/self-improvement/engine.ts:431 (literal instruction string targeting intent-classifier.ts); src/core/agent/intent-classifier.ts SPAWN_KEYWORDS / CONVERSATION_PATTERNS, header "Adapted from ChatGPT Agent output (2026-04-03)"; consumers src/core/agent/loop.ts:52 and src/core/learning/routing-trace.ts:21. Full classification in docs/SCAFFOLDING_AUDIT.md (S-U3). | Give learned routing a first-class store (a learned-routing table or eval-backed policy) so the doctrine can retire the keyword scaffold without also deleting the learning sink attached to it. Today the two are the same file, which makes the scaffold undeletable for the wrong reason.
